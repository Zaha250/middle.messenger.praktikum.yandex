export default class BaseApi {
    protected create() { throw new Error('Метод пуст'); }

    protected request() { throw new Error('Метод пуст'); }

    protected update() { throw new Error('Метод пуст'); }

    protected delete() { throw new Error('Метод пуст'); }
}
